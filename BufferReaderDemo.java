// BufferReaderDemo.java// 缓冲区的出现是为了提高流的操作效率// 所以在创建缓冲区之前必须先有流对象import java.io.*;import java.lang.String;class BufferReaderDemo{	public static void main(String []args)throws IOException{		// 创建一个读入流对象		FileReader fr = new FileReader("Text\\file_demo.txt");		// 加入缓冲技术		/* 两种构造函数		 * BufferedReader(fw)       缓冲区默认大小		 * BufferedReader(fw,len)   缓冲区指定大小		 */		BufferedReader br = new BufferedReader(fr);		while(true){			// readLine不会返回文件中的回车符，只返回其他字符			// 如果要写入的时候用newLine()			String s1 = br.readLine();  // 读一行,达到流末尾返回null			if(s1==null)				break;			System.out.println("s1:" + s1);		}				// 关闭关联资源，关闭缓冲区的实质就是在关闭流		br.close();	}}